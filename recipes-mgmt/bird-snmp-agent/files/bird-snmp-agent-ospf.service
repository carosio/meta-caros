[Unit]
Description=SNMP-Agent for OSPF state of BIRD Routing Daemon
Requires=bird-snmp-agent.service

[Service]
Environment=OSPFMIBFILE=@LIBEXEC@/OSPF-MIB.txt
SyslogIdentifier=bird-snmp-agent
ExecStart=@SBINDIR@/bird-snmp-agent-ospf
Restart=always

[Install]
WantedBy=multi-user.target
